version: '3.3'

services:

  web:
    hostname: web
    image: "stuartshay/azuredevopskats:devops"
    environment:
     - ASPNETCORE_ENVIRONMENT=Docker
    ports:
     - "5500:5000"
    networks:
      - azuredevopskats-network

networks:
  azuredevopskats-network:

